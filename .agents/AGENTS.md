# Workspace Customization Rules

## Git Commands Policy
* **CRITICAL**: Never execute `git commit`, `git push`, or any command that stages, commits, or pushes changes to the repository automatically.
* You must always ask the user for explicit permission in the current conversation turn before executing any Git staging, commit, or push command.
