export const metadata = {
  title: "Direct Store Checkout | TIVAA POS",
  description: "Internal store POS checkout session for processing direct in-store purchases.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function DirectStoreSaleLayout({ children }) {
  return <>{children}</>;
}
