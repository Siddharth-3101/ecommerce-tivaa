export const metadata = {
  title: "Return & Refund Policy | TIVAA",
  description: "Read our return, replacement and refund policy for eligible products purchased from TIVAA.",
};

export default function RefundPolicyLayout({ children }) {
  return <>{children}</>;
}
