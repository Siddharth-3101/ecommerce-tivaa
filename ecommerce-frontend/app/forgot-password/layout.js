export const metadata = {
  title: "Reset Password | TIVAA",
  description: "Reset your TIVAA account password securely and regain access to your shopping account in just a few steps.",
};

export default function ForgotPasswordLayout({ children }) {
  return <>{children}</>;
}
