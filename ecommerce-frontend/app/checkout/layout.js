export const metadata = {
  title: "Secure Checkout | TIVAA",
  description: "Complete your purchase securely with multiple payment options and fast order processing at TIVAA.",
};

export default function CheckoutLayout({ children }) {
  return <>{children}</>;
}
