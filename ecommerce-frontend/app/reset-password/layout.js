export const metadata = {
  title: "Set New Password | TIVAA",
  description: "Set a new secure password for your TIVAA account to regain full access to online shopping, saved items, and order tracking.",
};

export default function ResetPasswordLayout({ children }) {
  return <>{children}</>;
}
