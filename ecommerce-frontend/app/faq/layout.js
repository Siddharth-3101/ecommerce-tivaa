export const metadata = {
  title: "Frequently Asked Questions | TIVAA",
  description: "Find answers to common questions about orders, payments, shipping, returns and shopping at TIVAA.",
};

export default function FaqLayout({ children }) {
  return <>{children}</>;
}
