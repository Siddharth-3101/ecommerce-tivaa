export const metadata = {
  title: "Create Your TIVAA Account",
  description: "Register a TIVAA account to shop online, track orders, save addresses and receive exclusive product updates.",
  robots: {
    index: false,
    follow: true,
  },
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
